- [x] Update app.js so initIntro() runs the same intro GSAP timeline on mobile as on desktop (remove the mobile early-return skip).

- [ ] Verify intro loader appears on mobile and completes full animation before main content reveals.
- [ ] Verify after completion the loader hides the same way as desktop.

