- [ ] Update `index.html` to replace the portfolio grid markup with a carousel viewport + animated track
- [ ] Update `style.css` with carousel styles (fixed card width, gap, overflow hidden, keyframes + paused state)
- [ ] Update `app.js`: replace `initCollectionGrid` with an infinite duplicated-items carousel renderer + pause/resume on hover/click
- [ ] Sanity check for modal click behavior on portfolio cards
- [ ] Run a quick manual verification: smooth continuous left scroll, seamless loop, pause on hover/click, resume on mouseleave

